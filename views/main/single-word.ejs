<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= foundWord.word %></title>
  </head>
  <body>
    <a style="font-size: 1.5rem" href="/words/get-words">Home</a>

    <div
      style="background-color: lightblue; padding: 10px; margin-bottom: 10px"
    >
      <h1><%= foundWord.word.toUpperCase() %></h1>
      <p><strong>Definition:</strong> <%= foundWord.meaning %></p>
      <button style="margin-bottom: 10px">
        <a style="text-decoration: none" href="/words/update/<%=foundWord.id%>"
          >Update Word</a
        >
      </button>
      <form
        action="/words/delete/<%= foundWord.id %>?_method=DELETE"
        method="POST"
      >
        <button type="submit">Delete</button>
      </form>
    </div>
    <div
      style="background-color: lightgray; padding: 10px; margin-bottom: 10px"
    >
      <h2>Add Comment:</h2>
      <form action="/comments/add-comment/<%= foundWord.id %>" method="POST">
        <textarea
          cols="30"
          rows="5"
          type="text"
          id="comment"
          name="comment"
        ></textarea
        ><br />
        <button type="submit">Add Comment</button>
      </form>
    </div>

    <% if(foundComments.length){ %>
    <div style="background-color: lightyellow; padding: 10px">
      <h3>Comments:</h3>
      <% foundComments.forEach(element => { %>
      <div>
        <p><%=element.date%></p>
        <p style="font-style: italic; font-size: 1.1em"><%=element.comment%></p>
        <hr />
      </div>
      <% })} %>
    </div>
  </body>
</html>
